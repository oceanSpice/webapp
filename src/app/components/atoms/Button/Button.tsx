import React, { ReactElement } from "react";

type ButtonProps = {
    onClick: any
    text: string
}

export default function Button({
    onClick,
    text
}: ButtonProps): ReactElement {
    return (
        <></>
    )
}