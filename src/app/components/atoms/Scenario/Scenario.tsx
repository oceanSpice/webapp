import React, { ReactElement } from "react"

type ScenarioProps = {
    name: string
    explanation: string
}

export default function Scenario({
    name, 
    explanation
}: ScenarioProps): ReactElement {
    return (
        <></>
    )
}